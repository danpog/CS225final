#include "song.h"
#include "playlist.h"
#include <vector>
#include <string>
#include <iostream>
#include <fstream>
using namespace std;

int main()  {


    return 0;
}

vector<Playlist>& parse(string filename)    {
    /*fstream file;
    while (file.good()) {
        string n;
        cin >> n; // takes in all the other inputs
    } */
}
